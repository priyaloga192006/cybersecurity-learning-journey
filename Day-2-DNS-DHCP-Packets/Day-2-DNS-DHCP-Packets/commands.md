# Commands Practiced

## nslookup

Purpose:
Finds the IP address of a domain.

Command:

```bash
nslookup google.com
```

## tracert

Purpose:
Shows the route packets take to reach a destination.

Command:

```bash
tracert google.com
```
